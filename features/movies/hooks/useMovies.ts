

/*
export const useMovies = ()=>{
    const dispatch = useDispatch()

    useEffect(()=>{
        if(usersQuery.data){
            console.log(usersQuery.data)
            dispatch(setMovies(usersQuery.data.data))
        }
    },[usersQuery.data,dispatch])


    return {
        usersQuery,
    }
}
    */